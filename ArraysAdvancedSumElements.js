function arraysAdvancedSumElements(input){
let firstEl = Number(input.shift());
let lastEl = Number(input.pop());
let sum = ((firstEl+lastEl))
    console.log(sum);
}
arraysAdvancedSumElements(['20', '30', '40']);
